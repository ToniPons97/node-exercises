import isEven from "./isEven.js";

const n = 5;
console.log(`${n} ${isEven(n) ? 'is even.' : 'is not even.'}`);

const m = 6;
console.log(`${m} ${isEven(m) ? 'is even.' : 'is not even.'}`);

const p = 7;
console.log(`${p} ${isEven(p) ? 'is even.' : 'is not even.'}`);
